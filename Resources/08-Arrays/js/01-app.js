// Object
const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true,
}


// Array
const numeros = [10, 20, 30];

// Otra forma de crear arrays
const meses = new Array('Enero', 'Febrero', 'Marzo');

// Podemos crear un arreglo con un solo elemento que contiene datos de todo tipo
const deTodo = ["Hola", 10, true, "Si", null, {nombre: 'Giggi,', trabajo: 'Graphic Designer & Front end developer'}, [1, 2, 3]];

console.log(numeros);
console.log(meses);
console.log(deTodo);