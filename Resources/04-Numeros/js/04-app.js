let resultado;

// Se ordenan automaticamente siguiendo las bases matematicas
resultado = 20 + 30 * 2;

// Darle orden determinado
resultado = ( 20 + 30 ) * 2;

// Porcentaje, 20% de descuento
resultado = ( 20 + 30 + 60 ) * .2;

// Impuesto 16%
resultado = ( 20 + 30 ) * 1.16;



console.log(resultado);