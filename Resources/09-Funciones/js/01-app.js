// Declaracion de funcion (Function Declaration)
function sumar(){ // Dentro de los parentesis se declara la funcion y dentro de las llaves lo que va a hacer la funcion
    console.log(2+2);
}

// Llamar la funcion 
sumar();
sumar();
sumar();